<Window x:Class="PadForge.Views.ProfileDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        ui:WindowHelper.UseModernWindowStyle="True"
        Title="Save As Profile"
        Height="240" Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Profile Name:" Margin="0,0,0,4"/>
        <TextBox Grid.Row="1" x:Name="NameBox" Text="New Profile" Margin="0,0,0,12"/>

        <TextBlock Grid.Row="2" Text="Executable Names (comma-separated, e.g. game.exe,game2.exe):"
                   Margin="0,0,0,4"/>
        <TextBox Grid.Row="3" x:Name="ExeBox" Margin="0,0,0,12"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Save" MinWidth="80" Margin="0,0,8,0"
                    IsDefault="True" Click="SaveButton_Click"
                    Style="{StaticResource AccentButtonStyle}"/>
            <Button Content="Cancel" MinWidth="80"
                    IsCancel="True" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
